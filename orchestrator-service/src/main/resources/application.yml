server:
  port: 8080

spring:
  kafka:
    bootstrap-servers: ${KAFKA_BROKER:localhost:9092}
    topic:
      teste: teste
    consumer:
      auto-offset-reset: latest
      group-id: orchestrator-group

logging:
  level:
    org:
      apache:
        kafka: OFF